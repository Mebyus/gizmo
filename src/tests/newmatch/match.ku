fun use_if_match(x: uint) => uint {
    if x
    => 1, 2, 4 { return 2; }
    => 3 {
        let a := 1;
        return a;
    }
    else { return x; }
}
/*
fun use_if_bool_match(x: uint) => uint {
    if
    => x == 1 {}
    => x > 2 { return 2; }
    => 2 * x < 10 { 
        let a := 2;
        return a + x;
    }
    else { return 1; }

    return 3;
}
*/