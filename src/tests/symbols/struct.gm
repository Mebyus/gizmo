type Counter struct {
    names: []str,
    c: int,
    string_field: *Counter,
}

fn [Counter] inc() => int {
    let c: int = g.c;
    g.c += 1;
    return c;
}

fn use_counter(c: *Counter) => int {
    return c.inc();
}

type A struct {
    b: *B,
}

type B struct {
    a: *A,
}
