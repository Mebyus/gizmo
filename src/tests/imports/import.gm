/*
import std {
    mem => "mem"
    os  => "os"
}
*/

import {
    fmt  => "tests/fmt"
    math => "tests/math"
}

/*
import pkg {
    http => "net/http"
    pool => "mebyus/pool"
}
*/

fun use_math_add(a: s32, b: s32) => s32 {
    return math.add(a, b);
}
