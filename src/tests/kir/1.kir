let foo: sint = -8;

var bar: u32 = +(1 + 0);

fun add2(a: u32, b: u32) => u32 {
    var x: u32 = a + b;
    var y: u32 = dirty;
    y = x;
    return y;
}

fun max_u32(a: u32, b: u32) => u32 {
    if a > b {
        return a;
    }
    return b;
}

fun add5(a: u32) => u32 {
    never;
    return add2(2, a) + add(3, 4);
}
